let ele;
function setup() {
  ele = createAudio('Assignment 4 audio.mp3');

  // here we set the element to autoplay
  // The element will play as soon
  // as it is able to do so.
  ele.autoplay(true);
}
// Audio is from a project that I did in which I recorded water
//link to reference: 
